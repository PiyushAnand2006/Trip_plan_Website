# Phases

## Phase 1 — Project Setup
- Create folder structure and docs.
- Initialize frontend with Vite + React.
- Initialize backend with Express.
- Connect CORS and local dev scripts.

## Phase 2 — Homepage Layout
- Build sticky TopNav.
- Build full-width hero banner.
- Build TopPicks cards.
- Build Search section.
- Build Packages grid + filters.
- Build Hotel carousel.
- Build Destination cards.
- Build Testimonials + Footer.

## Phase 3 — State & Data
- Convert static arrays into reusable data files.
- Lightweight client-side fetch from local data (or fake APIs).
- Add loading/error states.

## Phase 4 — Interactions
- Wishlist toggle.
- Filter chips.
- Smooth scroll behavior.
- Carousel navigation.

## Phase 5 — Backend API
- Package routes (`/api/packages`, `/api/packages/:id`).
- Bookings route scaffold.
- CORS + error middleware.

## Phase 6 — Polish
- Responsive refinements.
- Accessibility checks.
- 3D movable banner transform effects.
- Performance audit and image optimization.

## Phase 7 — Deployment Prep
- Build scripts and env separation.
- Static deploy config.
- Documentation for future maintainer.
